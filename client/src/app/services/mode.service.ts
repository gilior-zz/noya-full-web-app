import {Mode} from '../../../../shared/models'
import {} from ''
export class ModeService {
    get currentMode(): Mode { return this.currentMode }

    set currentMode(mode: Mode) { this.currentMode = mode; }
}
